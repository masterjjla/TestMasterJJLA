import { UploadTask } from "electron-publish";
import { PlatformPackager } from "../platformPackager";
export declare class RemoteBuilder {
    build(targets: Array<string>, unpackedDirectory: string, packager: PlatformPackager<any>, outDir: string): Promise<any>;
}
export interface ArtifactInfo extends UploadTask {
    target: string | null;
    readonly isWriteUpdateInfo?: boolean;
    readonly updateInfo?: any;
}
